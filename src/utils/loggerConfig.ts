// Nothing needed here anymore - the logger is self-configuring
export function initializeLogger() {
  // Logger is already configured by default
}

export function cleanupLogger() {
  // No cleanup needed
} 